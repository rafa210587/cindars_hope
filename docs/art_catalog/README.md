# Catálogo de Arte — Cindar's Hope

> Cópias organizadas da arte do projeto para referência e para não perder (versionado no git).
> **Estes são CÓPIAS.** As originais que o Unity usa ficam em `Assets/_Game/Art/**` e
> `Assets/_Game/Resources/**` — NÃO mova/renomeie os originais (quebra GUID/referências).

## Estrutura
- `world_farm/` — crops, construção modular, casas modulares, animais (arte da fazenda)
- `world_cave/` — tiles/props por bioma de caverna
- `_reference_keyart_GPT/` — keyart de referência recuperada do ChatGPT (projeto "Sprites - Fazendeiro")

## Fontes
1. Projeto (`Assets/_Game/**`) — já versionado.
2. ChatGPT web ("Sprites - Fazendeiro" / chat "Arte pixel fazenda") — keyart perdida localmente, recuperável.
3. Histórico/memória — nomes e notas do que deveria existir.
